//PET API ENDPOINTS
const PET_API_ENDPOINTS = {
     PET: '/pet',
};

//STORE API ENDPOINTS
const STORE_API_ENDPOINTS = {
     INVENTORY: '/store/inventory',
};

export const { PET } = PET_API_ENDPOINTS;
export const { INVENTORY } = STORE_API_ENDPOINTS;
